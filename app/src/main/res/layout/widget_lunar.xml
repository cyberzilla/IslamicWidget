<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/widget_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:src="@drawable/widget_bg_shape"
        android:importantForAccessibility="no" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:padding="2dp"
        android:gravity="center"
        android:layoutDirection="ltr">

        <!-- Moon Phase Image (always visible, centered) -->
        <ImageView
            android:id="@+id/iv_moon_phase"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:scaleType="fitCenter"
            android:adjustViewBounds="true"
            android:importantForAccessibility="no" />

        <!-- Date Info Column (hidden at 1x1, shown when wider) -->
        <LinearLayout
            android:id="@+id/container_date_info"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1.5"
            android:layout_gravity="center_vertical"
            android:orientation="vertical"
            android:gravity="center_vertical"
            android:paddingStart="8dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/tv_hijri_date"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#FFC107"
                android:textSize="15sp"
                android:textStyle="bold"
                android:text="Memuat hijriyah..."
                android:maxLines="1"
                android:ellipsize="end"
                android:shadowColor="#B3000000"
                android:shadowRadius="3"
                android:shadowDx="1"
                android:shadowDy="1" />

            <TextView
                android:id="@+id/tv_gregorian_date"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#E0E0E0"
                android:textSize="12sp"
                android:text="Memuat tanggal..."
                android:maxLines="1"
                android:ellipsize="end"
                android:layout_marginTop="2dp"
                android:shadowColor="#B3000000"
                android:shadowRadius="3"
                android:shadowDx="1"
                android:shadowDy="1" />

            <TextView
                android:id="@+id/tv_moon_phase_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#B0BEC5"
                android:textSize="11sp"
                android:text="Moon Phase"
                android:maxLines="1"
                android:ellipsize="end"
                android:layout_marginTop="2dp"
                android:shadowColor="#B3000000"
                android:shadowRadius="3"
                android:shadowDx="1"
                android:shadowDy="1" />

        </LinearLayout>
    </LinearLayout>
</FrameLayout>
